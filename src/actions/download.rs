use std::collections::HashMap;

pub fn download_packages(packages: Vec<String>, versions: HashMap<String, String>) -> Vec<String> {
    let mut paths: Vec<String> = Vec::new();

    println!("Now online installing isn't supported!");

    return paths
}